# Required
DISCORD_TOKEN=

# Discord guilds (comma-separated IDs)
GUILD_IDS=

# JSON state storage (templates, raids, etc.)
DATA_PATH=data/state.json

# Bank database configuration
# Prefer BANK_DATABASE_URL. If empty, code falls back to DATABASE_URL.
BANK_DATABASE_URL=
DATABASE_URL=
BANK_SQLITE_PATH=data/bank.sqlite3

# Permissions
RAID_REQUIRE_MANAGE_GUILD=true
RAID_MANAGER_ROLE_ID=
BANK_REQUIRE_MANAGE_GUILD=true
BANK_MANAGER_ROLE_ID=
BANK_ALLOW_NEGATIVE=true

# Scheduler / raid timings
SCHED_TICK_SECONDS=15
DEFAULT_PREP_MINUTES=10
DEFAULT_CLEANUP_MINUTES=30
VOICE_CHECK_AFTER_MINUTES=5
